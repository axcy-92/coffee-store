package com.bse.backend.assignment.coffeestore.security.api.model;

/**
 * Enum representing roles for authentication and authorization in the Coffee Store application.
 */
public enum Role {
    USER,
    ADMIN
}